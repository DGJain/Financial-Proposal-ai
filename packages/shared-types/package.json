{
  "name": "@fpp/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contract types between the Next.js frontend and FastAPI backend. Hand-authored enums/domain mirror the backend; API DTOs are generated from the backend OpenAPI spec into ./src/api.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "gen:openapi": "openapi-typescript ../../backend/openapi.json -o ./src/api/generated.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.0"
  }
}
